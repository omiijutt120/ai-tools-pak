Daily SEO/GEO/AEO research and site-health audit for https://aitoolspak.tech/ (repo: /home/ubuntu/ai-tools-pak, GitHub: omiijutt120/ai-tools-pak, deployed via GitHub Pages from main). You are running on an Oracle Cloud Ubuntu VM; the system timezone is Asia/Karachi. Your tools are terminal and file.

ENVIRONMENT (Linux): use `python3` (or `python`), plain `curl`, `node`, `git`. For file searches inside the repo use `grep -r` (no search_files tool here). Do not use the clarify tool — this job runs unattended. This machine has Hermes installed at ~/.hermes (that is you). The repo clone lives at /home/ubuntu/ai-tools-pak (branch main); the trend history file is /home/ubuntu/seo_daily_history.json; the reports repo is /home/ubuntu/aitoolspak-seo-reports (a git repo that already has a README; commit reports into its root).
CRITICAL ANTI-HANG RULE (job died 2026-08-06 with idle timeout 620s): append `--max-time 20` to EVERY curl call in this runbook (e.g. `curl -s --max-time 20 URL`). If a fetch times out, record "timeout" and move on — NEVER retry a hanging command more than once, and never run a single terminal command that can take >60s. Keep SERP/DDG queries to at most 5 at a time with a hard cap of 3 attempts total per day per query — captcha-blocked engines are recorded as "throttled" and skipped.

STEP 1 — Pull latest: run `git -C /home/ubuntu/ai-tools-pak pull --ff-only` (if the clone is missing, `git clone https://github.com/omiijutt120/ai-tools-pak.git /home/ubuntu/ai-tools-pak`).

STEP 2 — Live health check: curl these URLs and report any that are NOT 200: https://aitoolspak.tech/ , /blog/ , /blog/ai-tools-pakistan-complete-guide/ , /blog/canva-pro-price-pakistan/ , /canva-pro-pakistan/ , /llms.txt , /llms-full.txt , /sitemap.xml , /robots.txt , /semrush-pro-pakistan/ , /chatgpt-plus-pakistan/ , /social-media-services/ , /enterprise-ai-api-credits/ . Fetch the homepage title: `curl -s https://aitoolspak.tech/ | grep -o '<title>[^<]*</title>' | head -1` and flag if it contains "Site not found". Record response times with `curl -s -o /dev/null -w "%{http_code} %{time_total}s"`.

STEP 2.5 — robots.txt AI-crawler check (GEO): fetch https://aitoolspak.tech/robots.txt and confirm it explicitly allows GPTBot, ClaudeBot, PerplexityBot, Google-Extended, ChatGPT-User, OAI-SearchBot, ByteSpider, CCBot and others (Allow: /), and that the Sitemap line is present. Flag if AI-crawler rules were removed or the file regressed to only `User-agent: *`.

STEP 3 — Repo audits (run from /home/ubuntu/ai-tools-pak): `node scripts/seo-audit.js`, `node scripts/check-site-links.js`, `node scripts/check-catalog.js`, `node scripts/check-social-services.js`. Then regenerate the derived files from data and commit + push ONLY if they changed (they are safe to auto-update):
- `node scripts/generate-llms-txt.js` and `node scripts/generate-llms-full-txt.js` (rebuild /llms.txt and /llms-full.txt — the full AI text dump; must stay fresh). If `git status` shows these changed, commit and push: `git add llms.txt llms-full.txt && git commit -m "Regenerate llms files" && git push`.
- `node scripts/generate-blog-ai-tools-guide.js` (rebuilds /blog/ai-tools-pakistan-complete-guide/ — the complete tools directory with all PKR prices + the AI API credits section; derived from products-data.js, safe to auto-update). If changed, commit and push: `git add blog/ai-tools-pakistan-complete-guide/ && git commit -m "Regenerate complete tools directory guide" && git push`.
- `node scripts/generate-products.js` (rebuilds sitemap.xml + product pages from CSV). If it rewrites sitemap.xml or pages due to real change, commit and push. A product-count mismatch or CSV/data drift is a REGRESSION — report it verbatim; do NOT force-regenerate on drift.

STEP 4 — Full per-page SERP ranking audit (DuckDuckGo HTML endpoint; if DDG throttles, wait 20s between queries and note which queries were throttled). Attempt `python3 /home/ubuntu/ai-tools-pak/scripts/cdp_serp.py "QUERY"` FIRST if a headless Chrome is reachable at 127.0.0.1:9223 (real Google SERP) — if it connects, use it for authoritative Google positions (wait ~5s). Otherwise fall back to `python3 /home/ubuntu/ai-tools-pak/scripts/seo_research.py ddg "QUERY"`. Audit the PRIMARY keyword for EVERY product page plus all guides, and record aitoolspak.tech rank/URL or "not found", plus the top 3-5 competitor domains. Keyword list: "chatgpt plus price in pakistan", "claude pro price in pakistan", "canva pro price in pakistan", "capcut pro price in pakistan", "semrush pro price in pakistan", "grok subscription pakistan", "gemini pro price in pakistan", "leonardo ai pakistan", "grammarly premium pakistan", "quillbot premium price pakistan", "heygen ai pakistan", "elevenlabs creator pakistan", "ai tools pakistan", "ai tools pak", "affordable ai tools in pakistan", "ai tools in pakistan price", "ai tools in pakistan 2026", "buy ai tools in pakistan", "ai tools directory pakistan", "ai api credits pakistan", "buy openai api credits pakistan", "openai api credits price pakistan", "claude api credits pakistan", "gpt api pakistan", "affordable api credits pakistan", "ai api key pakistan", "api credits wholesale pakistan", "social media services price in pakistan", "instagram followers price in pakistan". ALWAYS also track the competitor domain **aitoolspakistan.pro** for the queries "ai tools pak", "ai tools pakistan", "ai tools pakistan price" and record whether it outranks aitoolspak.tech.

STEP 5 — Indexation check: `python3 /home/ubuntu/ai-tools-pak/scripts/seo_research.py ddg "site:aitoolspak.tech"` and count indexed URLs; compare against yesterday from /home/ubuntu/seo_daily_history.json (read it; if missing, create it). Google Search Console status: report whether the site appears verified/connected on this machine (look for GSC/credentials config; if none, note "GSC not verified — owner action required"). ALWAYS update the history JSON with today's numbers (health, audits, SERP ranks incl. competitor, indexation count, GSC status, asset sizes, notes). Keep it valid and compact.

STEP 6 — Performance: attempt `python3 /home/ubuntu/ai-tools-pak/scripts/seo_research.py psi "https://aitoolspak.tech/"` (expect HTTP 429 from this datacenter IP; if 429, note "PSI rate-limited" and instead record TTFB + homepage asset sizes via curl HEAD: styles.css, script.js, products-data.js, logo.png, social-services-data.js, social-media-services/social-services.js — flag anything > 100KB or TTFB > 500ms).

STEP 6.5 — API CREDITS GROWTH MONITOR (orders focus; strategy doc: /home/ubuntu/ai-tools-pak/oracle/api-credits-growth-strategy.md):
- curl the landing page https://aitoolspak.tech/enterprise-ai-api-credits/ and confirm 200 + Service schema + FAQPage schema still present (`grep -c '\"@type\": \"Service\"'` and FAQPage).
- Confirm the conversion elements are still on the page (curl + grep): the use-case quick-select buttons (`class="use-case-buttons"`), the token-planning table (caption `Typical monthly token usage by use case`), the `What to prepare before ordering` card, and at least 3 WhatsApp CTAs (`wa.me/923714549245`).
- curl the API how-to article https://aitoolspak.tech/blog/how-to-buy-ai-api-credits-pakistan/ and confirm 200.
- Confirm the strategy doc still exists in the repo (oracle/api-credits-growth-strategy.md).
- From the STEP 4 SERP results, flag any API-credits keyword where a competitor outranks us and name the competitor domain.
- DRAFT ONE ready-to-send WhatsApp promo message for API credits (2-3 lines, mentions bulk tokens, providers, WhatsApp number +92 371 454 9245, and a soft CTA). It is a DRAFT for the owner to review and send — NEVER send messages or publish content automatically.

STEP 7 — GEO/AEO freshness:
- fetch https://aitoolspak.tech/llms.txt and confirm "Current PKR Prices" (~31 products), the "Full documentation" line linking to /llms-full.txt, and the complete-directory guide link
- fetch https://aitoolspak.tech/llms-full.txt and confirm it is a large plain-text dump (tens of KB) covering all products and blog guides
- fetch https://aitoolspak.tech/blog/ai-tools-pakistan-complete-guide/ and confirm it lists ALL 31 tools (count `tool-entry` occurrences via curl | grep -c), each with a PKR price and an Order-on-WhatsApp link, AND that the "AI API Credits for Businesses & Developers" section is present
- fetch https://aitoolspak.tech/canva-pro-pakistan/ and verify it has FAQPage + Product schema and a visible "Canva Pro price in Pakistan" FAQ section
- fetch https://aitoolspak.tech/semrush-pro-pakistan/ (FAQPage + Offer PKR), https://aitoolspak.tech/social-media-services/ (FAQPage + ItemList + 6 platform cards), https://aitoolspak.tech/enterprise-ai-api-credits/ (Service + FAQPage schema)

STEP 8 — Publish: write the full markdown report to /home/ubuntu/aitoolspak-seo-reports/$(date +%F).md, then `git -C /home/ubuntu/aitoolspak-seo-reports add -A && git -C /home/ubuntu/aitoolspak-seo-reports commit -m "Daily SEO research $(date +%F)" && git -C /home/ubuntu/aitoolspak-seo-reports push`. If the push fails, say so explicitly.

YOUR FINAL RESPONSE IS THE REPORT — a compact markdown summary:
1. Health: any 404s/regressions/site-down (call these out first, loudly)
2. Audit results (pass/fail each) + whether llms files / tools-directory guide / sitemap were regenerated & pushed
3. SERP snapshot: a compact per-keyword list (keyword → rank/URL or not-found → top competitor) — specifically flag any page that is NOT ranked, and whether aitoolspakistan.pro outranks us on "ai tools pak*"
4. Indexation count + trend + GSC verification status
5. Performance signals
6. GEO/AEO status (llms.txt, llms-full.txt, robots AI rules, complete-guide page, canva page schema)
6b. API Credits Growth Monitor: landing page 200/schema + each conversion element (use-case buttons, token table, prepare checklist, WhatsApp CTAs) pass/fail; API how-to article health; API-credit keyword ranks (from STEP 4) with competitor flags; then the DRAFTED WhatsApp promo message for API credits in a quote block, clearly marked "DRAFT — owner reviews and sends"
7. Recommended implementations for the next working session, ranked by impact (concrete: exact URLs/keywords/fields). Do NOT make content changes beyond the derived-file regeneration described in STEP 3.
Keep it compact (bullet lists, no fluff). The same text is saved as the daily report file in STEP 8.
